# Piscine Epitech – 2 premiers jours

Ces deux journées de la piscine Epitech m'ont permis de renforcer mes bases en environnement Unix et en scripting bash. J'ai réalisé chaque exercice proposé et conservé les scripts dans le dossier associé.

## 📁 Jour 01 – Unix Environment

- Découverte de l’environnement Linux en ligne de commande
- Création de fichiers et répertoires avec permissions spécifiques
- Utilisation de `ln` pour créer des liens symboliques
- Script `mr_clean` : suppression ciblée de fichiers via `find`
- Script `push_that.sh` : automatisation du versioning Git
- Reproduction d’un arbre de dossiers complexe avec `tree` et `ln`
- Compression d’un dossier avec `tar` (gzip)

## 📁 Jour 02 – Shell scripting (Bash)

- `how_many_are_we.sh` : compter les étudiants à partir d’un CSV
- `find_sh.sh` : rechercher tous les fichiers `.sh` récursivement
- `count_files.sh` : compter les fichiers normaux dans un dossier et ses sous-dossiers
- `gotta_catch_them_all.sh` : filtrage de noms depuis `/etc/passwd`
- Ajout de scripts personnels au `$PATH`
- `skip.sh` : affichage d’une ligne sur deux
- `r_tacpy.sh` : filtrage et traitement avancé de texte avec environnement

📄 Les énoncés sont disponibles dans ce dossier au format PDF.  
📜 Les scripts sont disponibles dans le sous-dossier [`scripts/epitech`](../../scripts/epitech) de ce portfolio.
