**Lab 2 :** 
- Deux réseaux: 
work.home.arpa et home.arpa
Un DHCP sur chaque, plages indiquées
- To do:
    - Effectuer un ping depuis LAN1 vers LAN2 et inversement (travail à effectuer sur le routeur)
    - Renseigner gateway sur serveur DHCP
    - Installer un serveur DNS sur LAN1 pour résoudre les adresses du LAN2 => debserver-lan1.home.arpa doit être capable de fournir l'adresse de debserve-lan2.home.arpa
    - Renseigner serveur DNS sur DHCP
